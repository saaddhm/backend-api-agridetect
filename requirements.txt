# --- Core API ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
sqlmodel==0.0.19
pydantic[email]==2.7.1
python-jose[cryptography]==3.3.0

# --- Auth / Password hashing ---
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- Database driver (MySQL en production) ---
PyMySQL==1.1.1            # DATABASE_URL=mysql+pymysql://user:pass@host/db

# --- Notifications push (chat de support). Optionnel : si absent/non configuré,
#     l'envoi FCM est ignoré sans bloquer l'API. ---
firebase-admin==6.5.0

# --- Image / Data ---
pillow==10.3.0
numpy==1.26.4            # IMPÉRATIF < 2.0 (compat TensorFlow 2.16 + ultralytics)
opencv-python-headless==4.10.0.84

# --- TensorFlow inference (CPU : plus léger, moins de RAM, pas de CUDA) ---
tensorflow-cpu==2.16.1
ultralytics==8.4.60

# --- Optional background removal ---
# Do not use with TensorFlow if it creates a numpy conflict.
# rembg
